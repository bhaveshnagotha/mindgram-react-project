import styled from 'styled-components'
import {
  background,
  border,
  color,
  layout,
  position,
  shadow,
  space,
} from 'styled-system'

export const Label = styled.label`
  ${color}
  ${layout}
  ${space}
  ${border}
  ${background}
  ${position}
  ${shadow}
`
